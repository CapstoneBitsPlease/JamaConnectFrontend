import Button from "@atlaskit/button"
import React from "react";

const Buttons = () => {
  console.log("We are here in the function"); 
  return ( 
    <Button>Default button</Button>
  );
}

export default Buttons;
